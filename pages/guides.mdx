import { getPagesUnderRoute } from "nextra/context";
import Link from "next/link";

export function BlogHeader() {
  return (
    <div className="max-w-screen-lg mx-auto pt-4 pb-8 mb-16">
      <h1>
        <span className="font-semibold leading-tight lg:text-5xl">Guides</span>
      </h1>
      <p className="text-center text-slate-500 dark:text-slate-400">
        In-depth and practical guides based on first hand <i>f*ckups</i> and experiences
      </p>
    </div>
  );
}

export function BlogIndex() {
  return getPagesUnderRoute("/guides").map((page) => {
    return (
      <div key={page.route} className="mb-10">
        <Link href={page.route} style={{ color: "inherit", textDecoration: "none" }} className="block font-semibold mt-8 text-2xl">
          {page.meta?.title || page.frontMatter?.title || page.name}
        </Link>
        <p className="text-slate-500 dark:text-slate-400" style={{ marginTop: ".3rem", lineHeight: "1.7rem" }}>
          {page.frontMatter?.description}{" "}
          <span className="inline-block">
            <Link className="nx-text-primary-600 no-underline hover:no-underline nx-decoration-from-font [text-underline-position:from-font]" href={page.route}>{"Read more →"}</Link>
          </span>
        </p>
      </div>
    );
  });
}

<BlogHeader />
<BlogIndex />